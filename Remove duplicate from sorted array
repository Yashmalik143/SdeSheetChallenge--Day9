int removeDuplicates(vector<int> &arr, int n) {
	// Write your code here.
    int count =n;
    for(int i =1;i<n;i++)
    {
        if(arr[i-1]==arr[i])
            count--;
            
    } return count;
}
